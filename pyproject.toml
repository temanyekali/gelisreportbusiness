 [project]
  name = "gelis-backend"
  version = "1.0.0"
  description = "GELIS Backend API"
  requires-python = ">=3.11"
  dependencies = []

  [build-system]
  requires = ["hatchling"]
  build-backend = "hatchling.build"

  [tool.hatch.build.targets.wheel]
  packages = ["backend"]
